using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TipCalculator : MonoBehaviour
{
    /*[SerializeField]
    private float _bill;
    [SerializeField]
    private float _tipAmount;
    [SerializeField]
    private float _totalAmount;

    // Start is called before the first frame update
    void Start()
    {
        _tipAmount = _bill * 0.2f;
        _totalAmount = _bill + _tipAmount;
        Debug.Log("Your bill is: " + _bill);
        Debug.Log("You must pay at least 20% tip. Your tip is: " + _tipAmount);
        Debug.Log("Your your total check: " + _totalAmount);
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
